# 新会话快速启动提示

> 上下文已耗尽，以下是你在新会话中需要知道的全部信息。
> **先把这个读完，再做任何其他事。**

---

## 紧急：上次会话留下了什么？

上一会话（2026-06-11）完成了 **命名空间记忆存储系统的全部设计工作**，但 **没有交付一行代码**。
你现在要做的第一件事：**把设计变成代码**。

实现计划在 `docs/superpowers/plans/2026-06-11-memory-store-mvp.md`
— 这是一个 **8 个 Task 的完整 TDD 计划**，每步测试先行，从 MemoryItem 模型到 MemoryManager 高层封装。

**注意**：DeepSeek API 无法使用子 Agent（code-reviewer/planner 等），直接用 Inline Execution 执行。

---

## 记忆存储 MVP 的 8 个 Task（串行，不可打乱）

| # | 内容 | 文件 |
|:-|:-----|:-----|
| 1 | MemoryItem Pydantic 模型 | `src/memory/store.py` + `tests/test_memory_store.py` |
| 2 | MemoryStore(ABC) 抽象接口 | 同上追加 |
| 3 | JsonFileStore put/get 实现 | 同上追加（7 测试） |
| 4 | JsonFileStore search/delete/list | 同上追加（7 测试） |
| 5 | 错误处理 + 边界情况 | 同上追加（4 测试） |
| 6 | MemoryManager 高层封装 | `src/memory/manager.py` + `tests/test_memory_manager.py`（6 测试） |
| 7 | 更新 `__init__.py` 导出 | `src/memory/__init__.py` |
| 8 | 全量回归 `make check` | `ruff + pyright + pytest` |

预计新增 ~24 测试，全量测试升至 ~326 passed。

---

## 项目关键事实（不要重新查）

| 事实 | 内容 |
|:----|:------|
| 技术栈 | Python 3.12+ / LangGraph / DeepSeek-Chat / Pydantic / akshare |
| 当前阶段 | Phase 1 MVP — data→debate 链路已就绪 |
| 测试现状 | 302 passed, 8 skipped |
| API Key | **缺失**（`.env` 不存在）→ 端到端验证阻塞 |
| 最新提交 | `9369d50` — 会话结束文档同步 |
| 远程 | origin/main 已同步（0 ahead） |
| 子 Agent | ❌ 不可用（DeepSeek API 限制） |

### 模块完成度（快速参考）
```
src/utils/      100% ✅     src/agents/      85%
src/core/        80%        src/memory/      75%（含 KB + SkillDisk）
src/data/       100% ✅     src/debate/     100% ✅
src/backtest/     0% ⬜     src/risk/         0% ⬜
```

---

## 关键文档索引

| 优先级 | 文档 | 用途 |
|:------:|:-----|:------|
| 🥇 | `docs/superpowers/plans/2026-06-11-memory-store-mvp.md` | **实现计划（先读这个）** |
| 🥇 | `docs/架构设计/2026-06-11-memory-store-design.md` | 设计文档（实现时对照） |
| 🥇 | `docs/技术债务与架构决策/架构决策记录.md` | ADR-011（架构决策依据） |
| 📋 | `docs/流程规范/AI自动化工作流程.md` | 标准操作流程 |
| 📋 | `docs/流程规范/AI会话交接文档.md` | 项目完整状态 |
| 📋 | `docs/ai-work-logs/2026/06/11/2026-06-11.md` | 上次会话详细记录 |

---

## 技术红线（不要违反）

1. **所有 LLM 调用必经 `src/utils/llm.py`** — 不得直接实例化 ChatDeepSeek
2. **Pydantic 作为模块间数据契约** — 跨模块传递用 BaseModel
3. **类型注解必须完整** — Pyright basic mode 零错误
4. **四同步原则** — 代码 + 测试 + 文档 + 债务日志同步更新
5. **重大决策先调研竞品再由人决策** — §1.3 流程规范

---

## 当你完成记忆存储 MVP 后

| 优先级 | 下一步 | 前置 |
|:------:|:-------|:-----|
| 🥇 | 端到端链路验证 `DebateOrchestrator.run()` | 需要 API Key |
| 🥈 | 记忆系统接入 MasterAgent/DebateOrchestrator | 记忆存储 MVP ✅ |
| 🥉 | 前端 MVP Streamlit 3 页面 | 端到端链路就绪 |

---

> 你可以把以上内容作为你的初始 prompt 输入新会话。
> 所有实现的代码质量门槛：`make check` 全通过。
