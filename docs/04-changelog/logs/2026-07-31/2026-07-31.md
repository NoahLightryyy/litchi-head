# 2026-07-31 AI 工作日志

## Batch A：CI #71 测试隔离恢复

### 触发与根因

- 2026-07-30 已将 KR-1B-3B 全部代码和交接提交推送至 `origin/main`；
- GitHub Actions Run #71 的前端、Ruff、Pyright 均通过，但 Python 3.12/3.13
  全量 Pytest 有三个后端辩论路由测试失败；
- 根因不是生产辩论逻辑，而是 `resolve_stock_name` 的 autouse mock 只放在
  `TestRunDebate` 类内。其他测试类也会 POST `/api/debate/run`，因此在 CI
  无外网环境中误访 AKShare/上交所；
- 会话跨到 7 月 31 日后，本地全量测试又暴露北交所生命周期样本的时间漂移：
  模拟响应截止于 2026-07-30，但查询截止日取系统当天。

### 修复

- `tests/test_backend/test_debate.py`：把股票名称查询 mock 提升为模块级 autouse
  fixture；成功解析和空名称 fail-closed 测试继续使用内层覆盖，不降低行为验证；
- `tests/test_data/test_official_lifecycle_provider.py`：只在对应样本内冻结
  `src.data.providers.bse_status.date.today()` 为 2026-07-30，使查询窗口与固定
  官方响应一致；
- 两项改动只消除测试对真实网络和系统日期的依赖，没有改变任何生产数据口径、
  风险阈值或失败关闭策略；
- Code Reviewer 复审批准，无 P0/P1/P2 问题；
- 修复提交：`a352fb0`（`test: isolate CI from network and clock drift`），已推送
  `origin/main`。

### 验证

- 专项回归：23 passed；
- `python scripts/check.py --full`：4/4 通过；
- Ruff：通过；
- Pyright：0 errors；
- 非慢测试：1459 passed、4 skipped、19 deselected；
- 前端 TypeScript 类型检查：通过；
- 推送前闸门再次得到相同结果；
- GitHub Actions Run #72：frontend、Python 3.12、Python 3.13 全部 success。

### 五同步与交接

- 代码：本批没有生产代码变化；
- 测试：完成后端路由网络隔离与生命周期样本时钟隔离；
- 文档：同步全局交接、质量保障部交接和工作日志索引；
- 债务：问题已在本批直接修复，无新增债务；
- 引用清理：已搜索旧 CI 状态描述并替换相关交接入口；
- 学习卡片：属于既有测试隔离纪律的缺口修正，无新增技术方案，不新增卡片；
- 下一原子仍固定为 KR-2 公司行动与统一复权，不因 CI 修复改变产品路线。

## Batch B：一流产品差距审视与决策 baseline 立项

### 主要判断

- 用主席方法完成“事实—主要矛盾—阶段—行动—反证”审视；
- 当前主要矛盾确认为：**系统建设能力很强，但真实结果验证能力很弱**；
- Phase R 88% 只能代表功能清单，不能代表实盘证据成熟度；
- 当前建设方向保持不变：先完成 KR-2～KR-6，再进入 4～8 周影子验证；
- 暂停用新增 Agent、指标或市场替代真实 baseline 闭环。

### 新增唯一协议

- 新建 `docs/02-requirements/DECISION_BASELINE_AND_SHADOW_VALIDATION.md`；
- 定义 B0 现金、B1 市场、B2 等权持有、B3 简单规则、B4 AI 消融五类对照；
- 定义预注册、不可变决策快照、1/5/20 日结果、成本/不可成交、S0/S1/S2 样本阶段；
- 指标分为预测、经济、安全和运行四张成绩单；
- 写入防挑样、防后验改规则、防未来信息、停止线和转段条件；
- 具体主基准、交易成本、阈值和资金上限仍须在实现前由用户确认。

### 五同步与引用清理

- 文档：README、ROADMAP、主 HANDOVER、跨部门总览及全部 11 部门 HANDOVER 已接入；
- 债务：新增跨部门关键债务 TD-074，路由总数 32→33；
- 引用：把“置信度机制已实现”和“真实概率已校准”分开；修正 README 极简复盘表述；
- 产品措辞：归档定位增加失效提示，在证据闭环前不再用“帮你盈利”作为当前承诺；
- 学习：新增 `39-decision-baseline-shadow-validation.md` 并更新索引；
- 生产代码：本批没有变化；
- 完整闸门首次暴露 `test_runtime_adapter_recovery_survives_persistently_failing_clock`
  把回放 `as_of` 固定在 2026-07-31，和故障路径回退的真实系统 UTC 发生跨日漂移；
- 测试改为使用同次提交返回的 `envelope.collected_at`，验证 SQLite
  `collected_at <= as_of` 的包含性边界；不改变生产回放、时钟回退或失败关闭语义；
- 独立 Code Reviewer 审查通过，P0/P1/P2 均为 0；
- 专项：1 passed；`python scripts/check.py --full`：4/4 通过；
- 最终闸门：Ruff 通过、Pyright 0 errors、1459 passed、4 skipped、19 deselected、
  前端 TypeScript 通过。

## Batch C：KR-2A 来源无关公司行动与点时复权核心

### 本批边界

- 只实现来源无关的版本化因子契约和确定性 QFQ 纯算法；
- 没有新增或更换任何数据源，没有实现联网 Provider；
- KR-2B 的“因子主源 + 独立公告核验源”保留为用户决策门。

### 代码与测试

- 新增 `src/data/kline_adjustment.py`：
  - `CorporateActionFactor` 保存 `known_at`、修订、供应商版本、精确股本比例、
    因子精度及因子/公告双路血缘；
  - 同版不同内容/血缘失败关闭，组合版本哈希绑定全部规范化因子内容；
  - `AdjustedDailyBar` 明示调整价、调整量和 RAW 成交额三种 basis；
  - `AdjustedKlineSeries` 绑定 KR-1 `raw_snapshot_id`、采集时间、
    `raw_completed_through` 与查询 `as_of`；
  - 现金只调价；送转/拆并股按精确比例核验价量；配股跨停牌不虚构 K 线；
  - Decimal 内部精度按输入有效位数确定，不受调用方 context 影响；
  - 新证据模型冻结，反序列化校验 OHLC、序列身份、日期、基准日和完成证明。
- 新增 `tests/test_data/test_kline_adjustment.py`，26 项合成契约覆盖无事件、现金、
  送转、3:1 拆股、错误价量、配股跨停牌、因子修订、来源冲突、内容哈希、时区、
  快照未来信息、Decimal 确定性和模型反序列化。
- `src/data/__init__.py` 只导出稳定的 KR-2A 领域入口，不导出 Provider 内部项。

### 审查驱动修正

- Code Reviewer 首轮发现组合版本未绑定实际内容、同版血缘顺序依赖、混合 basis
  不可见等问题，均已修复；
- Python Reviewer 阻断了“只凭 15:00 墙钟认定完成”和“只校验价量方向”两项
  金融风险；最终改为 KR-1 完成证明绑定、精确股本比例与来源精度校验；
- 最终两路审查均无 P0/P1；派生序列直接反序列化的完成证明 P2 也已补齐。

### 五同步与交接

- 文档：README、ROADMAP、主/跨部门/数据部 HANDOVER、ADR-013 和 K 线实施计划
  均拆分为 KR-2A 已完成 / KR-2B 待来源确认；
- 债务：TD-069 仍开放，KR-2A 记为已偿进展；KR-2B～KR-6 未完成；
- 学习：新增 `40-point-in-time-adjustment.md` 并补前后链接；
- 引用清理：搜索 KR-2、下一原子和“代码尚未实现”等旧状态，防止下个部门重复
  KR-2A 或误报整个 KR-2 完成；
- 下一动作：向用户提交 KR-2B 来源组合决策，不越权接入网络数据源。

### 最终验证

- KR-2A 专项：26 passed；
- KR-1 evidence/store + KR-2A：78 passed；
- `python scripts/check.py --full`：4/4 通过；
- Ruff：通过；Pyright：0 errors；
- 后端非慢测试：1485 passed、4 skipped、19 deselected；
- 前端 TypeScript：通过。

## Batch D：KR-2B-1 新浪累计前复权因子证据快照

### 决策与边界

- 用户确认方案 A：沪深累计前复权除数主源固定为新浪 `qfq.js` 直连，
  CNINFO/交易所正式披露作为独立公司行动核验；
- Tushare、BaoStock、AKShare 包装层不进入当前链路；北交所在独立官方事件源
  完成前失败关闭；
- 本批只完成累计因子证据快照，不生成 `CorporateActionFactor`，不进入
  AI/API/回测；下一原子固定为 KR-2B-2 官方事件条款匹配。

### 代码与测试

- `src/data/evidence.py` 新增彼此隔离的 `CUMULATIVE_QFQ_FACTOR` 与
  `CORPORATE_ACTION_FACTOR` 能力；新浪来源只声明前者，供组合运行时消费但不
  满足最终公司行动因子完整性；
- `src/data/kline_adjustment.py` 新增冻结的
  `CumulativeQfqFactorPoint/QfqFactorSnapshot`，校验代码市场、内容哈希、日期顺序、
  基准除数和最新单位锚点；
- `src/data/providers/sina_adjustment.py` 直连并严格解析新浪响应，保存原始字节
  SHA-256、字节数、版本、时间和 Decimal 精度；不使用 `eval`；
- 网络失败、脏响应和 BSE 官方核验缺失分别使用
  `upstream_request_failed`、`invalid_upstream_payload`、
  `official_verification_unavailable`；
- `tests/test_data/test_sina_adjustment_provider.py` 26 项契约覆盖合法快照、当前签名
  包装、计数、日期、哨兵、锚点、无事件历史、模型不变量、非法代码、原始 bytes、
  JSON 类型/重复键漂移、完成时间、网络失败和 BSE 触网前关闭；与 KR-2A 合计
  52 passed；
- 真实冒烟：`000001`/`600000` 成功取得 31/28 个累计点及响应哈希；
  `920016` 返回 `UNSUPPORTED` 且不请求新浪。

### 审查驱动修正

- Code Reviewer 阻断了“请求开始时刻冒充证据可得时刻”和非法代码触网；改为响应
  完成后记录时间，并在 Provider/模型两层校验明确 A 股号段；
- Python Reviewer 阻断了累计快照冒充公司行动能力、字符串响应伪造原始字节、
  JSON 数值丢失 Decimal 词法精度；最终拆分 capability、强制 bytes 和固定字符串；
- 复审进一步明确累计能力应可供 KR-2B-2 标准证据链消费，因此不设
  `discovery_only`；它仍无法满足 `CORPORATE_ACTION_FACTOR` policy；
- 补齐重复 JSON key、哨兵前事件、解析异常边界及反序列化精度不变量；两路最终
  复审无 P0/P1/P2。

### 五同步与交接

- 文档：README、ROADMAP、实施计划、ADR-013、主/跨部门及所有相关部门 HANDOVER
  已改为“KR-2B-1 完成，KR-2B-2 下一原子”；
- 债务：TD-069 保持开放，只核销累计快照子项，不误报 KR-2 完成；
- 学习：新增 `41-cumulative-factor-vs-corporate-action.md`；
- 引用清理：Provider 改为语义明确的 `sina_adjustment.py`，全项目清理旧模块名和
  “来源待确认”执行态；Batch C 的历史记录保留为当时事实；
- 下一动作：复用 CNINFO 公告分页/下载/哈希，实现官方公司行动条款、同日事件
  聚合及相邻累计除数到事件因子的受控转换。

### 验证

- KR-2A + KR-2B-1 专项：52 passed；
- 两路最终复审：无 P0/P1/P2；
- `python scripts/check.py --full`：4/4 通过；
- Ruff：通过；Pyright：0 errors；
- 后端非慢测试：1511 passed、4 skipped、19 deselected；
- 前端 TypeScript：通过。
