# 2026-07-29 AI 工作日志

## Batch A：CNINFO 公开端点直连三态门禁

### 用户决策

- 批准“直连巨潮公开查询端点，AKShare 保留为可替换适配器”；
- 确认后续采用“多个通道采集并汇总 → 统一格式打包 → 传给其他业务节点”的边界；
- 本批只完成 CNINFO 直连原子功能，不同时实现 `DataEvidenceService` 或正式辩论接入。

### TDD

- RED `d9fd22c`：新增 `test_cninfo_direct_provider.py`，因
  `CninfoDirectAnnouncementSource` 不存在而按预期失败；
- 测试数据修正 `169f4d9`：修正上海时间对应的毫秒时间戳，不改写 RED 检查点；
- GREEN `98cbf57`：实现 `cninfo-direct` 适配器和公开端点分页调用；
- 直连专项 + 原 AKShare 适配器：18 passed；
- 数据层回归：280 passed、4 skipped。

### 实现结果

1. 直接调用巨潮股票列表和公告查询公开端点，全程使用 HTTPS、显式超时和状态码检查；
2. 显式读取并校验 `totalAnnouncement`，不再通过 AKShare 空 DataFrame 推断零结果；
3. `totalAnnouncement=0` 且公告列表为空时返回 `SUCCESS_EMPTY`；
4. 总数与公告数量矛盾、字段损坏或网络失败时返回可见 `FAILED`；
5. 支持多页公告完整拉取并校验最终数量；
6. `cninfo-direct` 与 `akshare-cninfo` 使用不同 `source_id`，但共享
   `upstream_id="cninfo"`，不会被汇总层误算成两个独立来源；
7. 旧 AKShare 适配器和既有调用保持可用。

### 真实来源门禁

平安银行 `000001`：

| 时间窗口 | 结果 | 条数 | 证据 |
|:--|:--|--:|:--|
| 2026-06-01～2026-07-28 | `SUCCESS_DATA` | 3 | 首条 `1225406051`，“董事会决议公告” |
| 2026-07-27～2026-07-28 | `SUCCESS_EMPTY` | 0 | 公开响应 `totalAnnouncement=0` |

网络超时通过故障注入测试验证为 `FAILED / upstream_request_failed`。

### 债务与同步

- TD-071 已关闭；
- 开放债务 32 → 31，已关闭 38 → 39；
- 更新 README、ROADMAP、ADR-013、全局/跨部门/数据交接、数据模块规格；
- 更新学习卡片 32，补充 CNINFO 直连案例和多通道统一汇总信封；
- 全项目清理“等待 CNINFO 路线决策”的当前引用，历史日志保持原始记录；
- coverage 跟踪仍会触发 TD-070 已记录的 AKShare→Pandas→NumPy 间歇导入失败，
  普通测试和完整项目闸门不受影响。

### 强制闸门

- Ruff：All checks passed；
- Pyright `src/ + backend/`：0 errors；
- Pytest：1288 collected；1265 passed、4 skipped、19 slow deselected；
- 前端 TypeScript：通过；
- 两条既有第三方警告继续由 TD-070 跟踪。

### 下一个原子功能

先给用户审阅 `DataEvidenceService` 的统一汇总信封字段，再按 TDD 实现：

- 查询范围；
- 按 capability 分组的标准化条目；
- 各通道状态和错误；
- 按 `upstream_id` 去重后的完整性评估；
- 供辩论、后端等业务节点使用的稳定打包边界。

本批到此停止，等待用户审阅。

## Batch B：DataEvidenceService 多通道统一汇总信封

### 用户确认

用户批准继续，并确认多个通道的数据先汇总、整理成统一格式，再打包传给其他业务
节点。本批只完成汇总服务和稳定信封，不接新闻新来源或正式辩论图。

### TDD

- RED `74989e7`：新增 7 项多通道汇总契约测试，因
  `src.data.evidence_service` 不存在而按预期失败；
- GREEN `dfd8ca3`：实现 `EvidenceEnvelope` 和 `DataEvidenceService`；
- 专项测试：7 passed；
- 数据层回归：287 passed、4 skipped。

测试覆盖：

1. 两个独立来源并发采集；
2. 输出顺序保持注册顺序，不受线程完成顺序影响；
3. 同一 `upstream_id` 保留全部来源诊断，但业务条目只输出第一份成功数据；
4. 未处理来源异常转换为可见
   `FAILED / source_unhandled_exception`；
5. `SUCCESS_EMPTY` 可满足查询成功且输出空条目；
6. request/policy capability 不匹配时在调用来源前拒绝；
7. 没有可用来源时返回不完整信封，而不是伪造成功。

### 统一信封

`EvidenceEnvelope` 向业务节点提供：

- 原始 `EvidenceRequest` 查询范围；
- `EvidencePolicy` 完整性门槛；
- 每个通道的 `SourceResult`、状态与错误；
- 去重后的标准化 `items`；
- `EvidenceAssessment` 独立上游评估；
- 总体 `complete` 和 `collected_at`。

业务节点无需知道 CNINFO、AKShare 或未来新闻通道的具体调用方式。

### 真实契约验证

CNINFO 真实零公告窗口通过 `DataEvidenceService` 后得到：

```text
complete=True
items=0
status=SUCCESS_EMPTY
successful_upstream_ids=["cninfo"]
```

证明真实适配器可直接进入统一信封，并且“成功但零条”不会被误判为证据断链。

### 文档、学习与债务

- 更新 ADR-013、数据模块规格、README、ROADMAP；
- 更新全局/跨部门/数据/后端交接和部门职责索引；
- 更新 TD-069 进度：汇总层完成，正式辩论失败关闭仍未接入；
- 更新学习卡片 32，补充真实 `EvidenceEnvelope` 字段和同上游条目选择规则；
- 无新增债务；
- coverage 再次命中 TD-070 已登记的 AKShare→Pandas→NumPy 跟踪导入问题，
  普通测试与项目闸门不受影响。

### 强制闸门

- Ruff：All checks passed；
- Pyright `src/ + backend/`：0 errors；
- Pytest：1295 collected；1272 passed、4 skipped、19 slow deselected；
- 前端 TypeScript：通过；
- 两条既有第三方警告继续由 TD-070 跟踪。

### 下一步

下一个原子功能选择首个独立免费新闻上游，先确认字段、时间窗口和上游身份，再按
`SourceResult[NewsItem]` 契约接入；第二个独立新闻源完成后配置完整性策略，随后才
接正式辩论失败关闭。

本批到此停止，等待用户审阅。

## Batch C：东方财富 + 新浪新闻双源并发聚合 API

### 用户确认

用户批准多个新闻通道并发采集，并通过单一接口拼接成统一业务信封。确认先接
东方财富与新浪；财联社因版权和跨节点传输边界暂缓。

### TDD

- RED `5362f3c`：新增新闻适配器、聚合 API、跨源去重共 12 项契约测试，因
  `src.data.providers.news` 不存在而按预期失败；
- GREEN `517212c`：实现两个独立新闻适配器和
  `POST /api/v1/evidence/news/aggregate`；
- 真实联调发现新浪公开历史未覆盖三天窗口，新增回归测试后修正为
  `STALE / time_window_not_fully_covered`；
- 专项最终 19 passed。

### 实现结果

1. 东方财富个股搜索按 `hitsTotal` 完整分页，严格做股票代码/名称关联与时间窗过滤；
2. 新浪财经快讯按页扫描，只有最老记录覆盖请求起点后才允许返回成功空；
3. 两个来源由 `DataEvidenceService` 并发执行，注册顺序不受完成顺序影响；
4. 相同股票和规范化标题跨来源去重，逐源原始结果仍保留在 `source_results`；
5. `NewsItem` 增加外部编号、精确发布时间、来源身份、发布媒体、关联依据和内容哈希；
6. 跨节点默认不传原始正文；
7. API 请求强制六位股票代码、带时区时间和正向时间范围。

### 真实来源门禁

平安银行 `000001`，最近三天：

| 来源 | 状态 | 条数 | 说明 |
|:--|:--|--:|:--|
| 东方财富 | `SUCCESS_DATA` | 4 | 个股搜索与时间窗过滤通过 |
| 新浪财经 | `STALE` | 0 | 可访问约 900 条历史仍未覆盖三天起点 |

最终 `complete=False`、`missing_independent_upstreams=1`。证明系统不会把“HTTP
成功但历史不足”误判为完整双源。

### 债务与同步

- TD-069 更新进度但不关闭：新闻双源已完成，正式辩论 LLM 前失败关闭仍待接入；
- 更新 ADR-013、数据与 FastAPI 模块规格、README、ROADMAP、全局/跨部门/数据/
  后端交接；
- 更新学习卡片 32，加入滚动信息流时间窗覆盖判定；
- 清理所有当前文档中的“新闻双源待接入”引用；历史日志保留原始记录；
- 无新增债务。

### 强制闸门

- Ruff：All checks passed；
- Pyright `src/ + backend/`：0 errors；
- Pytest：1307 collected；1284 passed、4 skipped、19 slow deselected；
- 前端 TypeScript：通过；
- coverage 跟踪再次触发 TD-070 已登记的 AKShare→Pandas→NumPy 间歇导入失败；
  普通测试与完整项目闸门不受影响；
- 两条既有第三方警告继续由 TD-070 跟踪。

### 下一步

把新闻 `EvidenceEnvelope` 接入正式辩论数据节点，并验证任一必需证据不完整时，
LLM 调用数为零。财联社继续等待版权和跨节点传输边界确认。

## Batch D：新浪滚动证据与正式辩论失败关闭

### 用户确认

用户确认固定最近 3 天新闻窗口：东方财富实时查询，新浪后台滚动采集并持久保存
元数据；缓存未连续覆盖完整窗口时返回 `EVIDENCE_INCOMPLETE`，不得调用 LLM。

### TDD

- RED `c9ceedb`：定义 SQLite 重启恢复、去重、3 天保留、断档重置、零 LLM 与
  HTTP 503 契约；
- RED `b05cf00`：定义新浪滚动源在覆盖不足时 `STALE`、完整时本地实体关联，以及
  元数据采集不传正文；
- GREEN `ad44176`：完成滚动存储、后台采集、双源运行时、LangGraph 条件门禁与
  503 映射；
- 相关数据、辩论和后端回归 59 passed；Ruff 与 Pyright 通过。

### 实现结果

1. `SqliteRollingNewsStore` 使用 WAL、`synchronous=FULL`、事务和
   `(source_id, external_id)` 唯一键；
2. 默认每 300 秒分页采集新浪当前可访问元数据（最多 20 页），保留 3 天；采集
   间隔超过 10 分钟重置连续覆盖起点；
3. `RollingNewsSource` 只有在请求窗被连续覆盖且末端足够新鲜时才返回成功；
4. 正式辩论固定最近 3 天，强制 `eastmoney + sina` 两个独立上游；
5. 证据不完整时 LangGraph 从 `collect_data` 直接结束，抛出
   `EvidenceIncompleteError`；
6. `/api/debate/run` 返回 503、`Retry-After: 300`、缺失上游和采集时间；
7. 专项测试确认分析师与大师 LLM 调用数均为零。

### 独立代码审查修正

代码审查发现并修正四项边界：

1. 辩论 API 先解析规范股票名称；名称缺失同样 503，避免新浪仅按代码误判可信空；
2. 新浪采集达到分页上限但未读完整体时抛出截断错误，不推进连续覆盖；
3. 轮询间隔在服务开始接流量前验证，后台任务异常结束时健康检查降为
   `degraded`；
4. 乱序旧批次只补写条目，不得倒退 `last_success_at`。

新增回归后相关数据、辩论、生命周期和后端测试为 64 passed，Ruff/Pyright 继续
通过。

### 强制闸门

- 首轮完整闸门暴露生命周期测试切换生产数据源的隔离问题，导致后续真实集成测试
  误用 adata；修复为 mock 配置、不污染全局数据源；
- 按原执行顺序专项复测 2 passed；
- 审查修正后的最终 `python scripts/check.py --full` 全绿：
  - Ruff：通过；
  - Pyright：0 errors；
  - Pytest：1321 collected，1298 passed、4 skipped、19 slow deselected；
  - 前端 TypeScript：通过；
- 两条既有第三方弃用警告仍由现有债务跟踪，无新增债务。

本批完成，等待用户审阅。下一原子方向是把实时行情、K 线和行业证据迁移到同一
失败关闭门禁。

### 债务与同步

- TD-069 新闻分支已完成失败关闭，但实时行情、K 线和行业证据尚未迁移，因此保持
  “修复中”；
- 更新数据、辩论、FastAPI 规格，ROADMAP、全局/部门交接、后端 README；
- 新增学习卡片 33《滚动证据与 Fail-Closed 门禁》；
- 无新增债务。

## Batch E：实时行情双源证据门禁

### 用户确认与标准校准

用户确认把实时行情作为下一个原子功能迁移到统一门禁。实施前对比深交所约 3 秒
Level-1 快照节奏，以及 Bloomberg、LSEG、IBKR、TradingView 的延迟/冻结/可疑
状态设计；市场标准强调时间戳、序列完整性和显式状态，不存在统一的“60 秒或
0.5% 价差”标准。

盘中对平安银行 `000001` 的东方财富、新浪直连接口并发采样 8 次：价格全部一致，
源时间差为 0 或 3 秒；同时确认东方财富成交量以“手”返回，新浪以“股”返回。
据此采用用户批准的产品策略：

- 连续竞价（9:30–11:30、13:00–14:57）数据年龄超过 10 秒即禁止 AI，超过
  30 秒标记硬过期；
- 两源时间差不得超过 3 秒；
- 时间对齐后价格差不得超过 1 个最小价位（A 股 0.01 元）；
- 午休、收盘和周末只允许旧展示接口展示，不启动新辩论；
- 东方财富、新浪任一缺失、陈旧或冲突均失败关闭，LLM 调用数为零。

### TDD

- RED `d51c933`：定义双源解析、单位归一化、时效、时间配对、价格冲突与休市门禁；
- GREEN `3680f9a`：完成 `EastmoneyQuoteSource`、`SinaQuoteSource` 和
  `RealtimeQuoteEvidenceService`，专项 12 passed；
- RED `9c0cc3e`：定义统一行情聚合 API 和正式辩论零 LLM 集成；
- GREEN `97ce9e6`：完成 `/api/v1/evidence/quotes/aggregate` 与正式图接入，
  集成专项 30 passed。

### 实现结果

1. 两个适配器直接连接真实东方财富与新浪上游，不把 AKShare 包装层计为独立源；
2. 保存上游行情生成时间，东方财富价格按分缩放、成交量从手归一化为股；
3. 单源网络或解析错误显式返回 `FAILED`，缺时间返回 `STALE`；
4. 双源错时或价格超差返回 `CONFLICTED`，统一信封保留逐源诊断；
5. 正式辩论优先使用双源校验后的唯一规范行情，不再调用旧全市场行情列表；
6. 任一行情门禁失败时 LangGraph 在首个 LLM 前结束，API 返回
   `EVIDENCE_INCOMPLETE`；
7. 旧 `/api/stocks/{code}/quote` 保持展示兼容，新业务节点使用统一证据接口。

### 独立代码审查修正

独立审查提出 5 项高风险边界，已全部按新增 RED 回归修正：

1. 编排器默认构造自动加载行情门禁，修复 FastAPI 之外的调用方可能失败开放；
2. 下午连续竞价终点从 15:00 修正为 14:57，收盘集合竞价只展示、不启动辩论；
3. 新浪适配器解析并校验响应变量中的真实证券代码，拒绝错股票响应；
4. 北交所旧 `4/8` 与新 `920` 号段统一路由到新浪 `bj`，东方财富使用
   `secid=0`；沪、深、北四个代表代码真实直连均返回正确身份；
5. 行情聚合接口新增可配置的每 IP 6 次/分钟限流。

审查回归 RED `058e57f`，GREEN `f132de9`；专项 32 passed，Ruff 和 Pyright 通过。

### 强制闸门

- 首轮完整闸门发现测试把集合序列化顺序写死，改为集合比较后专项 6 passed；
- 最终 `python scripts/check.py --full` 全绿：
  - Ruff：通过；
  - Pyright：0 errors；
  - Pytest：1347 collected，1324 passed、4 skipped、19 slow deselected；
  - 前端 TypeScript：通过；
- 两条既有第三方弃用警告不变，无新增债务。

### 债务与同步

- TD-069 实时行情分支已完成失败关闭；K 线和行业证据仍未迁移，因此保持“修复中”；
- 更新数据、辩论、FastAPI 规格，ADR-013、ROADMAP、全局/部门交接与后端 README；
- 新增学习卡片 34《实时行情对账：新鲜不等于一致》；
- 清理当前文档中“实时行情仍待迁移”的引用；历史日志保留原始记录；
- 无新增债务。

### 下一步

按一次一个原子功能继续 TD-069：先迁移 K 线证据，再迁移行业证据。
