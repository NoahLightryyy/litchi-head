# 2026-07-29 AI 工作日志

## Batch A：CNINFO 公开端点直连三态门禁

### 用户决策

- 批准“直连巨潮公开查询端点，AKShare 保留为可替换适配器”；
- 确认后续采用“多个通道采集并汇总 → 统一格式打包 → 传给其他业务节点”的边界；
- 本批只完成 CNINFO 直连原子功能，不同时实现 `DataEvidenceService` 或正式辩论接入。

### TDD

- RED `d9fd22c`：新增 `test_cninfo_direct_provider.py`，因
  `CninfoDirectAnnouncementSource` 不存在而按预期失败；
- 测试数据修正 `169f4d9`：修正上海时间对应的毫秒时间戳，不改写 RED 检查点；
- GREEN `98cbf57`：实现 `cninfo-direct` 适配器和公开端点分页调用；
- 直连专项 + 原 AKShare 适配器：18 passed；
- 数据层回归：280 passed、4 skipped。

### 实现结果

1. 直接调用巨潮股票列表和公告查询公开端点，全程使用 HTTPS、显式超时和状态码检查；
2. 显式读取并校验 `totalAnnouncement`，不再通过 AKShare 空 DataFrame 推断零结果；
3. `totalAnnouncement=0` 且公告列表为空时返回 `SUCCESS_EMPTY`；
4. 总数与公告数量矛盾、字段损坏或网络失败时返回可见 `FAILED`；
5. 支持多页公告完整拉取并校验最终数量；
6. `cninfo-direct` 与 `akshare-cninfo` 使用不同 `source_id`，但共享
   `upstream_id="cninfo"`，不会被汇总层误算成两个独立来源；
7. 旧 AKShare 适配器和既有调用保持可用。

### 真实来源门禁

平安银行 `000001`：

| 时间窗口 | 结果 | 条数 | 证据 |
|:--|:--|--:|:--|
| 2026-06-01～2026-07-28 | `SUCCESS_DATA` | 3 | 首条 `1225406051`，“董事会决议公告” |
| 2026-07-27～2026-07-28 | `SUCCESS_EMPTY` | 0 | 公开响应 `totalAnnouncement=0` |

网络超时通过故障注入测试验证为 `FAILED / upstream_request_failed`。

### 债务与同步

- TD-071 已关闭；
- 开放债务 32 → 31，已关闭 38 → 39；
- 更新 README、ROADMAP、ADR-013、全局/跨部门/数据交接、数据模块规格；
- 更新学习卡片 32，补充 CNINFO 直连案例和多通道统一汇总信封；
- 全项目清理“等待 CNINFO 路线决策”的当前引用，历史日志保持原始记录；
- coverage 跟踪仍会触发 TD-070 已记录的 AKShare→Pandas→NumPy 间歇导入失败，
  普通测试和完整项目闸门不受影响。

### 强制闸门

- Ruff：All checks passed；
- Pyright `src/ + backend/`：0 errors；
- Pytest：1288 collected；1265 passed、4 skipped、19 slow deselected；
- 前端 TypeScript：通过；
- 两条既有第三方警告继续由 TD-070 跟踪。

### 下一个原子功能

先给用户审阅 `DataEvidenceService` 的统一汇总信封字段，再按 TDD 实现：

- 查询范围；
- 按 capability 分组的标准化条目；
- 各通道状态和错误；
- 按 `upstream_id` 去重后的完整性评估；
- 供辩论、后端等业务节点使用的稳定打包边界。

本批到此停止，等待用户审阅。
