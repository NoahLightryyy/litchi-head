# 2026-08-03 AI 工作日志

## Batch A：TD-072 分时历史影子回填与正式量能基线底座

### 产品决策与调研

- 用户允许历史接口回填，并确认采用“影子回填 + 正式层失败关闭”；
- 实网验证腾讯可返回 2026-07-28～31 四个完整日，每日常规时段242点；
- 新浪相同日期缺少 `09:30/13:00/14:58/14:59` 等分钟，累计量偏差最高约
  1.26%；东方财富五日接口当前断连，因此不设置经验百分比容差；
- 对标 TradingView、QuantConnect、Fidelity、交易所授权行情与专业数据规范后，
  决定单源历史只做影子验证，正式信号继续要求可信规范数据。

### 实现

- 新增 `src/data/intraday_history.py`：
  - `single_source_shadow / dual_source_verified` 两级 Pydantic 契约；
  - 242个常规分钟、正成交量、单调累计值、双源身份门禁；
  - 市场/证券/日期分区的内容寻址 Parquet + SQLite 哈希清单；
  - 最近20个正式完整日的同分钟累计量中位数；
  - 成员缺失、篡改、解码失败和行数不符均失败关闭。
- 新增 `src/data/providers/intraday_history.py`：腾讯五日影子回填，官方交易日历
  覆盖，过滤盘后扩展行，成交量从手转股，当日未完成数据不入库；
- `src/data/intraday_runtime.py` 与分时 API 接入协调器：每日回填一次影子历史，
  双源完整日自动进入正式层；影子尚未晋升时返回
  `relative_volume_backfill_shadow_only`；
- 新增8项历史契约并扩展后端接口契约；同步 ADR、模块规格、债务、交接、路线图、
  Backend README 与学习卡片44。

### 验证

- TDD RED：缺模块、缺协调器、缺分钟/停牌日/未分区行为均先观察到预期失败；
- 专项测试：33 passed（历史、战况、双源分时、后端 evidence）；
- 真实烟测：4个影子日均242点，持久化后正式基线为 `None`；
- Ruff 目标文件通过；Pyright 目标模块 0 errors；
- `python scripts/check.py --full`：最终复跑4/4通过；1608 collected，1585 passed、
  4 skipped、19 slow deselected；Ruff passed；Pyright 0 errors；前端 TypeScript
  passed。两条既有第三方弃用警告不变。

### 债务

- TD-072 代码本金已结清，但债务不关闭：仍需自然积累20个双源完整日，并完成
  影子同分钟偏差、信号排序和误报率验证后才能正式启用 Relative Volume。

### 收尾

- 已同步根 README、Backend README、数据模块 README、全局路线图、全局/数据/质量
  交接页、ADR、模块规格、债务账本、学习卡片与工作日志索引；
- 已搜索并清理当前态文档中的旧测试基线和“L1 分时一期待实现”表述；历史日志不改写；
- 推送策略：本次仅提交到本地 `main`，不推送远端，等待用户确认。
