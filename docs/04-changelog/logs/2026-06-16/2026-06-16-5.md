# AI 工作日志 — 2026-06-16 (5) Sprint 6 K 线真渲染 + 数据源造假全面清除

> 本会话：Batch 3 — Sprint 6 Lightweight Charts K 线真渲染 + 数据源诚信审计

## 本次工作

### 1. Sprint 6: Lightweight Charts K 线真渲染

**创建 `candlestick-chart.tsx`**：
- lightweight-charts 纯封装层，不负责数据获取
- 暗色主题配色（#141420 背景，绿涨红跌）
- K 线蜡烛图 + 成交量直方图双序列（下方子图）
- `ResizeObserver` 自适应容器宽度
- 组件卸载清理（`chart.remove()`）

**重写 `kline-chart.tsx`**：
- 自包含数据获取（`useKline(code, period)`），只需传入 `code`
- 周期切换按钮（日线/周线/月线）真正切换 `period` 参数调用后端
- 三态覆盖：加载中（骨架脉冲）/ 错误（⚠️）/ 空数据（📭）/ 正常渲染

**简化 `page.tsx`**：
- 去掉外部 `useKline`、`useStockNews` 冗余调用
- `KlineChart` 接口简化：从 `data + loading` → 只需 `code`

### 2. 数据源造假全面清除（⚠️ 严重问题）

前端审计发现 `debate-panel.tsx` 硬编码 4 位大师辩论结果（含"模拟数据"标签）。

| 文件 | 问题 | 修复 |
|:-----|:-----|:-----|
| `debate-panel.tsx` | 硬编码 4 位大师分析数据 + "模拟数据"标签 🔴 | 删除造假 → 接入 `useRunDebate` + `useDebateResult` 真实 API hook |
| `market.py` | `_MOCK_SECTOR_HEAT` 8 个板块 heat/top_stocks 硬编码 🔴 | 删除 `_MOCK_SECTOR_HEAT`，`_enrich_sector_item` 用默认值 |
| `market.py` | 板块详情硬编码"示例股票"+"示例公司A-D"产业链 🔴 | 只返回板块 id/name |
| `market.py` | 宏观简报 `risk_tips`/`hot_topics` 硬编码 🔴 | 只返回真实指数行 |
| `chain-map.tsx` | `"宁德时代" => "300750"` 硬编码代码映射 🔴 | 删除，改为纯文本显示 |

### 3. 全量数据源诚信审计

审计范围：`frontend/` + `backend/` 全部文件。
审计结果：**零处造假数据** ✅

- 空就是空（`暂无数据`）
- 未实现就是未实现（`待实现：MA/RSI/MACD/布林带详情`）
- 错误如实显示（`数据加载失败，请检查后端服务`）

### 4. 后端增加 TD-020 数据增强缺失债务

移除造假后，以下字段在 akshare 不直接提供，需要接入真实数据源：
- `SectorItem.heat` / `top_stocks` — 板块热度排行
- `SectorDetail.chain_map` — 产业链上下游映射
- `SectorDetail.ai_analysis` — AI 板块分析
- `MacroBrief.risk_tips` / `hot_topics` — 市场风险提示

已登记为 TD-020。

### 5. 最终验证结果

| 检查项 | 结果 |
|:-------|:----:|
| Pyright backend/ | 0 errors ✅ |
| `pnpm build` | 零错误通过 ✅（3 页面路由正常） |
| `tsc --noEmit` | 零错误 ✅ |
| 数据源造假 | 0 处 ✅ |
| 前端 `http://localhost:3000` | ✅ 200 |
| 后端 `http://localhost:8000` | ✅ Health OK |

## 当前项目状态

Python 721 tests 不变。前端 K 线图已从占位 "📈" 升级为 Lightweight Charts 真渲染。全项目零造假数据。

### 待办（未完成）
- [ ] trust.py 信任度路由完整实现（占位→接入 TrustTracker）
- [ ] stocks/capital-flow 路由完整实现（占位→接入资金流向数据）
- [ ] 技术指标/资金流向/信任度 tab 面板从占位到实现
- [ ] TD-020: 板块数据增强层（heat/chain_map/ai_analysis 接入真实数据源）

## 债务变更

| 债务 | 操作 | 说明 |
|:----|:----|:-----|
| TD-020 🆕 | **新增** | 后端板块/产业链数据增强层缺失 — `SectorItem.heat/top_stocks`、`SectorDetail.chain_map`、`MacroBrief.risk_tips/hot_topics` 在移除造假后无真实数据源替代 |

## 待办事项
- [x] 创建 CandlestickChart 组件（lightweight-charts 封装 + 暗色主题 + 成交量直方图 + ResizeObserver）✅
- [x] 重写 KlineChart 组件（自包含数据获取 + 周期切换 + 三态覆盖）✅
- [x] 简化 stock page（KlineChart 接口从 data+loading → code）✅
- [x] pnpm build + tsc 零错误验证 ✅
- [x] 清除 debate-panel.tsx 硬编码大师数据 + "模拟数据"标签 ✅
- [x] 清除 market.py _MOCK_SECTOR_HEAT 硬编码板块数据 ✅
- [x] 清除 market.py 板块详情"示例股票"+"示例公司"硬编码 ✅
- [x] 清除 market.py 宏观简报 risk_tips/hot_topics 硬编码 ✅
- [x] 清除 chain-map.tsx 硬编码代码映射 ✅
- [x] 全量数据源诚信审计（frontend + backend）✅
- [x] 更新 debt 日志：新增 TD-020 ✅
- [x] 更新 HANDOVER.md / ROADMAP.md ✅

## 交接建议

下个会话执行 `/resume-session` 后：
1. 启动后端 `cd e:/litchi-head && uvicorn backend.main:app --reload --port 8000`
2. 启动前端 `cd frontend && pnpm dev`
3. **后端完善** — trust.py 信任度路由 / capital-flow 资金流向路由完整实现
4. **技术指标/资金流向/信任度 tab 面板** — 3 个占位 tab 实现
5. **TD-020**: 板块数据增强层接入真实数据源

> **上下文标记**：正常结束，新会话执行 `/resume-session` 继续
