# AI 工作日志 — 2026-06-16 (9) Batch Loop 四方向全完成

> 本会话：Batch Loop — 按 HANDOVER §5 优先级逐个推进

## 本次工作

### 🥇 1. 技术指标 Tab

**后端 `backend/indicators.py`**
- 纯 Python 离线计算四个指标（无 numpy/ta-lib 依赖）
- `calc_ma()` — SMA 5/10/20/60，递推窗 O(n)
- `calc_rsi()` — 14 日 RSI，首周期 SMA 种子后 EMA 递推
- `calc_macd()` — 12/26/9 标准 MACD
- `calc_bollinger()` — 20 日布林带（SMA ± 2σ）
- `calc_all()` — 聚合接口返回最新值摘要 + 全序列

**后端 `backend/routers/stocks.py`**
- 新增 `GET /api/stocks/{code}/technical-indicators`

**前端** — 全链路（类型 + API + Hook + 组件 + 接入）:
- `TechnicalIndicatorsPanel` — 四宫格布局：MA 卡片（4 线值）、RSI 卡片（数值+刻度条+超买/超卖标签）、MACD 卡片（DIF/DEA/Histogram）、布林带卡片（上/中/下轨+价格位置指示）
- 底部详细卡片：MA 排列研判（多头/空头/交叉）+ MACD 研判

### 🥇 2. TD-020 板块数据增强层

**后端 `backend/routers/market.py` 完整重写**：

| 增强点 | 方案 |
|:-------|:-----|
| 板块排行 change_pct/fund_flow | `ak.stock_board_industry_name_em()` 直接获取涨跌幅+主力净流入 |
| 成分股列表 | `ak.stock_board_industry_cons_em(symbol)` 获取真实个股行情 |
| heat 热度 | 成分股涨跌比计算：≥60%→high, 40-60%→medium, <40%→low |
| chain_map 产业链映射 | 涨幅分层：龙头层(前20%)/中坚层(中60%)/基础层(后20%) |
| ai_analysis AI 分析 | 从成分股数量/涨跌比/平均涨跌幅自动生成格式化分析 |
| ai_rating 个股评级 | 涨跌幅→A(≥5%)/B+(≥2%)/B(≥0)/C(≥-3)/D |

前端无需改动 — 已有组件正确消费所有增强字段。

### 🥇 3. 数据源生产配置

- **adata 2.9.5 安装**（`python -m pip install adata`）
- **`src/data/collector.py`** — `DataCollector` 新增类变量 `default_source`，所有模块的 `DataCollector()` 调用自动继承
- **`backend/config.py`** — 生产配置模块，支持环境变量 `LITCHI_DATASOURCE` = auto/akshare/adata
  - `auto` 模式：尝试 FallbackSource(ADataSource → AKShareSource)，adata 未安装时静默降级
- **`backend/main.py`** — lifespan 启动时自动调用 `setup_production_source()`
- **`backend/requirements.txt`** — 添加 adata>=2.9.5

### 🥈 4. 暗色主题打磨

| 改进项 | 说明 |
|:-------|:-----|
| 侧边栏路径高亮 | `usePathname()` 对比当前路径，活跃导航项高亮蓝色 |
| Header 动态标题 | "宏观总览" / "板块 · BKxxxx" / "个股 · 000001" 自动跟随 |
| 全局加载进度条 | 路径变化时顶栏蓝色进度条动画（400ms 过渡） |
| layout 架构优化 | 恢复 Server Component 导出 metadata，客户端逻辑分离到 `AppShell` |
| 错误态重试按钮 | 股票页 + 板块页错误状态增加"重新加载"按钮 + "返回"按钮 |
| 面包屑修复 | 移除指向 `/sector/000000` 的硬编码板块链接 |

## 最终验证

| 检查项 | 结果 |
|:-------|:----:|
| Pyright backend/ | 0 errors ✅ |
| pnpm build | 4 routes all passed ✅ |
| Python 单元测试 | 676 passed, 无回归 ✅ |
| 指标计算验证 | MA/RSI/MACD/Bollinger 全部正确 ✅ |
| adata 安装 | 2.9.5 ✅ |

## 文件变更

| 文件 | 操作 | 说明 |
|:-----|:----:|:-----|
| `backend/indicators.py` | 🆕 | 技术指标计算模块 |
| `backend/routers/stocks.py` | 📝 | +technical-indicators endpoint |
| `backend/routers/market.py` | 📝 | TD-020 板块数据增强重写 |
| `backend/config.py` | 🆕 | 生产数据源配置模块 |
| `backend/main.py` | 📝 | lifespan 启动配置 |
| `backend/requirements.txt` | 📝 | +adata>=2.9.5 |
| `src/data/collector.py` | 📝 | +default_source 类变量 |
| `frontend/app/app-shell.tsx` | 🆕 | 客户端布局外壳（高亮+进度条+动态标题） |
| `frontend/app/layout.tsx` | 📝 | 恢复 Server Component |
| `frontend/app/stock/[code]/page.tsx` | 📝 | 错误态+Tab 过渡 |
| `frontend/app/sector/[id]/page.tsx` | 📝 | 错误态重试按钮 |
| `frontend/components/stock/technical-indicators-panel.tsx` | 🆕 | 技术指标面板 |
| `frontend/components/stock/index.ts` | 📝 | +TechnicalIndicatorsPanel export |
| `frontend/lib/types/stock.ts` | 📝 | +技术指标类型 |
| `frontend/lib/api/stocks.ts` | 📝 | +fetchTechnicalIndicators |
| `frontend/lib/hooks/use-stock.ts` | 📝 | +useTechnicalIndicators |

## 债务变更

无新增债务。

## 交接建议

1. 当前所有 4 个优先级已完成，无待办 Blocking 任务
2. 建议下一步：**浏览器验证 UI** — `cd backend && python -m uvicorn backend.main:app --port 8000 && cdfrontend && pnpm dev`
3. 或开始 Phase 2 工作（前端测试/回测可视化/辩论日志管理）

> **上下文标记**：正常结束，新会话执行 `/resume-session` 继续
