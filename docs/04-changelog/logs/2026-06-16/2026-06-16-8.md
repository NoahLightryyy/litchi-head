# AI 工作日志 — 2026-06-16 (8) 后端路由完善 + Tab 面板实现

> 本会话：Batch 6 — capital-flow 资金流向路由 + trust.py 信任度路由 + 前端 Tab 面板

## 本次工作

### 1. capital-flow 资金流向路由完整实现

`backend/routers/stocks.py` — 从占位 `{"data": []}` 改为真实数据：

| 新增 | 说明 |
|:-----|:------|
| `CapitalFlowItem` | Pydantic 响应模型（date/main_net_inflow/retail_net_inflow/institutional_net_inflow） |
| `_detect_market()` | 股票代码→市场代码（sh/sz/bj）判定 |
| `_capital_flow_from_akshare()` | akshare `stock_individual_fund_flow` 核心调用，列映射（主力→main、小单→retail、大单→institutional） |
| 安全取值 | `safe_float`/`safe_str` 绕 pandas Series 类型推断 |

### 2. trust.py 信任度路由完整实现

`backend/routers/trust.py` — 从占位 `{"data": None}` 重构为 `TrustTracker` 接入：

| 新增 | 说明 |
|:-----|:------|
| `TrustReportResp` | 响应模型对齐前端 TrustReport 类型（win_rate/brier_score/confidence_bias 等） |
| `_get_tracker()` | 惰性创建 TrustTracker + JsonFileStore（避免 torch 等重型依赖） |
| `_trust_report_to_resp()` | `src.debate.trust.TrustReport` → `TrustReportResp` 映射 |
| `GET /api/trust/report/{agent_name}` | 查询单个大师信任度报告 |
| `GET /api/trust/leaderboard` | 信任度排行榜（按 win_rate 降序） |

### 3. 前端 Tab 面板接入

| 组件 | 说明 |
|:-----|:------|
| `capital-flow-panel.tsx` | 🆕 资金流向 Tab — 汇总卡片（主力/机构/散户 3 格）+ 近期明细列表 |
| `trust-chart.tsx` | 已存在组件，接入 stock 页 trust tab（leaderboard → 大师卡片网格） |
| `useTrustLeaderboard` hook | 🆕 TanStack Query hook 封装 leaderboard API |

### 4. 验证结果

| 检查项 | 结果 |
|:-------|:----:|
| Pyright backend/ | 0 errors ✅ |
| TypeScript frontend/ | 0 errors ✅ |
| pnpm build | Compiled successfully (4 routes) ✅ |
| Ruff lint (backend/) | 4 auto-fixed, 0 remaining ✅ |
| Python tests | 738 passed, 4 skipped（无回归）✅ |

## 债务变更

无新增债务。所有修改都有完整的类型注解和错误处理。

## 待办事项
- [x] capital-flow 资金流向路由（akshare 接入 + _detect_market + CapitalFlowItem 模型）✅
- [x] trust.py 信任度路由（TrustTracker 接入 + report + leaderboard）✅
- [x] 前端 CapitalFlowPanel 组件（汇总卡片 + 明细列表）✅
- [x] 前端 TrustChart 接入 stock 页 trust tab ✅
- [x] useTrustLeaderboard hook ✅
- [x] HANDOVER.md 状态 + 下一步同步 ✅
- [x] memory/current-state 同步 ✅
- [x] Pyright + TypeScript + pnpm build 全链路验证 ✅

## 交接建议

下个会话执行 `/resume-session` 后：
1. 技术指标 Tab（MA/RSI/MACD/布林带） — 当前唯一占位 tab
2. TD-020 板块数据增强层（heat/chain_map/ai_analysis）
3. `pip install adata` 后切换 ADataSource 做主源配置

> **上下文标记**：正常结束，新会话执行 `/resume-session` 继续
