# 2026-07-28 AI 工作日志

## Batch A：数据生命周期、容量与 Redis + SQL 调研

### 方向

用户提出三个实盘底座问题：

1. 数据库存多久、能承载多大数据；
2. 股市数据和 Agent 数据并发进入时能承受多大压力；
3. 业务链条与前后端接口能否严格匹配。

用户随后确认按“先基线、后选型、再迁移”的计划执行，并要求参考同行优秀产品。
本批只建立证据，不迁移真实数据库。

### TDD

- RED：新增 `tests/test_scripts/test_storage_baseline.py`，因基准模块不存在而按预期失败。
- RED commit：`3029379 test: define storage capacity baseline contract`。
- GREEN：实现 `scripts/storage_baseline.py`，比较 JSONL、JSON Object 和 SQLite WAL。
- GREEN commit：`ad60941 feat: add reproducible storage capacity baseline`。
- 回归 RED：复用同一基准目录时，JSONL 会累计旧记录并造成结果漂移。
- 回归 RED commit：`e3ee0df test: reproduce reused baseline directory drift`。
- 回归 GREEN：每次运行显式重置 JSONL、JSON Object 与 SQLite 基准数据。
- 回归 GREEN commit：`1cb6b1e fix: keep storage baseline reruns deterministic`。
- 专项测试：8 passed。
- 基准工具覆盖率：82.61%。
- Ruff：通过。
- Pyright：0 errors。

### 强制闸门

- `python scripts/check.py --full`：4/4 通过。
- 1231 collected；1208 passed、4 skipped、19 slow deselected。
- Ruff：通过。
- `src/ + backend/` Pyright：0 errors。
- 前端 TypeScript：通过。

### 本地容量结果

1,000 条、每条约 4 KB：

| 后端 | 写入 ops/s | 查询 p95 |
|:--|--:|--:|
| JSONL | 51,259 | 9.548 ms |
| JSON Object | 52.9 | 7.471 ms |
| SQLite WAL | 19,448 | 0.0142 ms |

### PostgreSQL / Redis 临时实测

- PostgreSQL 17：
  - `pgbench` 10 clients / 4 threads / 20 秒：约 1,438 TPS，0 failed；
  - 10,000 条不可压缩约 4 KB JSONB：约 764 ms；
  - 主键查询约 0.086 ms；
  - 表和索引约 54 MB。
- Redis 7：
  - 4 KB payload / 100,000 requests / 50 clients；
  - SET 约 221k requests/s，p95 约 0.215 ms；
  - GET 约 238k requests/s，p95 约 0.183 ms。

临时容器使用固定名称且无数据卷；测量后已核对名称并停止，未留下测试数据库。

### 同行调研

- Freqtrade：SQLite 默认保存交易事实，可通过 SQLAlchemy 切 PostgreSQL。
- TradingAgents：长期决策日志与按 ticker SQLite checkpoint 分离。
- NautilusTrader：有界内存缓存 + Redis/PostgreSQL 可插拔后端 + Parquet 行情目录；
  durable event store 是事实权威，cache 是可重建投影。

### 文档与决策

- 新增 `docs/02-requirements/STORAGE_LIFECYCLE_RESEARCH.md`。
- 新增 ADR-012：SQL 事实源 + Redis 协调层 + Parquet 行情目录，状态为待用户验收。
- 新增学习卡片 29：SQL 事实源与 Redis 可重建投影。
- 更新 ROADMAP、HANDOVER、ADR 索引和部门交接。

### 新增债务

- TD-066：JSON/JSONL 无生命周期、事务与跨进程安全。
- TD-067：后端 OpenAPI 与前端手写类型存在契约漂移。
- TD-068：重型辩论无 durable queue、全局背压与恢复。

开放债务仪表盘由 26 更新为 29；三个问题都已登记到对应部门。

### 当前停止点

Batch A 只建立基线和提议，不部署 PostgreSQL/Redis，不改变真实数据。下一步必须先由
用户确认生命周期默认值和 ADR-012，再进入真实辩论容量、session 恢复和故障注入门禁。
