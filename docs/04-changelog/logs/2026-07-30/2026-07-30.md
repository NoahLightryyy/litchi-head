# 2026-07-30 AI 工作日志

## Batch A：盘中 AI 双时间尺度方向固化

### 用户确认

用户明确要求：当天日 K 尚未收盘不能导致 AI 整个交易日停摆。盘中 AI 应同时使用
历史完整日 K、实时行情、已结束分钟和正在形成的今日动态状态，并把这条方向落实到
所有相关部门文档。

### 设计结论

1. 上一交易日及以前、已收盘且通过多源核验的日 K 标记为 `FINAL_DAILY`；
2. 今日已结束分钟标记为 `FINAL_MINUTE`；
3. 双源核验的当前行情标记为 `LIVE_QUOTE`；
4. 正在形成的当前分钟和今日 OHLC 标记为 `PROVISIONAL`；
5. 开盘后 AI 使用上述四层输入，不等待日 K 收盘；
6. `PROVISIONAL` 不得混入历史完整日 K，不得冒充已确认收盘形态；
7. 正式日线指标只使用 `FINAL_DAILY`；含盘中估算的指标必须独立字段、独立标签；
8. 收盘且多源确认后，今日动态状态才能晋升为 `FINAL_DAILY`；
9. 历史日 K 或盘中必需证据不完整时，仍在首个 LLM 调用前失败关闭。

### 跨部门同步

- ADR-013：新增盘中 AI 双时间尺度决策和明确不做项；
- ROADMAP、全局交接、跨部门交接：更新当前目标和下一原子功能；
- 数据管道部：定义四层证据信封、收盘晋升与正式/估算指标隔离；
- 辩论引擎部：定义上下文分区、提示词语义与零 LLM 门禁；
- AI Agent 架构部：禁止把 `PROVISIONAL` 写成收盘定论；
- 后端 API 部：目标响应分字段返回，并延续 HTTP 503 错误契约；
- 前端部：动态蜡烛、正式指标与盘中估算必须有可见标签；
- 模块规格：同步数据、辩论、Agent 编排、FastAPI 和前端目标；
- 各相关部门技术规范与模块 README/API：补充可执行约束和当前状态；
- TD-069：记录已确认产品方向，继续保持修复中；
- 学习卡片 35：补充“日 K 未收盘时 AI 如何工作”。

引用清理同时修复数据与前端部门交接中两处既有未闭合代码块，避免后续内容被错误
渲染为代码。

### 范围边界

本批只固化跨部门方向，没有提前写入尚未确认的 K 线具体上游、复权算法或冲突阈值，
也没有修改生产代码。下一步应先确认这些实盘默认值，再按 TDD 实现。

### 强制闸门

- `python scripts/check.py`：3/3 通过；
- Ruff：通过；
- Pyright：0 errors；
- 本批只有文档变更，无相关测试需要执行。
