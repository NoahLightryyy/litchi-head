# 2026-08-04 AI 工作日志

## Batch A：TD-075 沪市官方公告兼容与 KR-2 最终验收

### 根因与 TDD

- 真实 `600000/688008` PDF 在“重要内容提示”和正文各重复一次相同相关日期表；
  旧代码按正则匹配次数判断唯一性，把合法重复误报为冲突；
- RED 先用 `600000/1224119803` 官方摘录复现；GREEN 改为日期对集合，相同值幂等
  归并，不同日期仍失败关闭；
- 真实 `688503` 修订链进一步暴露“（修订版）”标题和 PDF 标题内部空格/换行；
  分别以参数化修订链和完整标题空白测试观察 RED，再只扩展明确标题与空白归一；
- `688503` 差异化正文以“总股本扣减回购股份后的股本”和“每股现金红利≈”表达；
  以官方摘录 RED 后增加精确公式布局，保留实际派发 `0.432` 与除权调整 `0.413`。

### 实现与真实验收

- `CNINFO_CORPORATE_ACTION_PARSER_VERSION` 升至 v4；
- 新增 `600000` 普通分红、`688503` 差异化修订版两份最小官方摘录夹具和 4 项行为
  契约；完整公告仍以来源 URL 与 SHA-256 留在事件血缘；
- `600000`、`688008` 分别完成普通/差异化 CNINFO → Sina → Tencent RAW 全链；
- `688503` 原公告 `1225365375` → 更正通知 `1225378572` → 修订版全文
  `1225378571` 回放为稳定 action ID、revision 3，并生成 `1e-12` 已核验价格因子；
- CNINFO 公告查询、Sina 累计因子、Tencent RAW 三处注入超时，全部无可用事件或
  因子；Sina/Tencent 明确返回 `upstream_request_failed`。

### 验证与收尾

- KR-2 专项：145 passed；Ruff 通过；目标 Pyright 0 errors；
- 完整闸门：1641 collected，1618 passed、4 skipped、19 deselected；Ruff、
  Pyright、Python 测试和前端类型检查 4/4 通过；
- TD-075 关闭，KR-2 公司行动与统一复权完成；下一阶段固定为 KR-3 四层业务信封；
- 已同步 README、路线图、实施计划、ADR、模块规格、债务、全局/各部门交接、
  triage、学习卡片 48、日志和引用；不推送远端。
