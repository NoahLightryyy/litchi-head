# AI 工作日志 — 2026-06-22(1) TD-039 API 速率限制 + triage Git Bash 兼容 + 三层测试策略

> 本会话：修复 TD-039（debate/run 限流）+ 补充 triage Git Bash on Windows 兼容模式 + 改进 resume-session Skill CI 检查命令 + 建立三层测试策略（@pytest.mark.slow）。

## 完成内容

### TD-039 API 速率限制（~30min ✅）

**问题**：`POST /api/debate/run` 无速率限制，被刷 = LLM 费用起飞。

**方案**：slowapi 三层限流 + 429 ErrorResponse 格式。

#### 文件变更

| 文件 | 操作 | 说明 |
|:-----|:----:|:-----|
| `pyproject.toml` | 📝 | +1 行：`slowapi>=0.1.9` |
| `backend/limiter.py` | 🆕 | 集中管理 Limiter 实例，支持 `enabled` 环境变量开关 |
| `backend/config.py` | 📝 | +`RATE_LIMIT_ENABLED` / `RATE_LIMIT_DEBATE_RUN` / `RATE_LIMIT_DEBATE_STATUS` / `RATE_LIMIT_DEBATE_RESULT` 环境变量 |
| `backend/main.py` | 📝 | +`app.state.limiter` 注册 + 429 `ErrorResponse` handler |
| `backend/routers/debate.py` | 📝 | 3 个路由加 `@limiter.limit()` 装饰器 + `request: Request` 参数 |
| `tests/test_backend/conftest.py` | 📝 | +`disable_rate_limit` autouse fixture，测试默认关限流 |
| `tests/test_backend/test_debate.py` | 📝 | +`TestRateLimit` 限流超限 429 + 响应格式 + GET 独立计数测试 |

#### 限流策略

| 端点 | 限流 | 环境变量 | 默认值 |
|:-----|:----:|:---------|:------:|
| POST /api/debate/run | **6/分钟** 每 IP | `LITCHI_RATE_LIMIT_DEBATE_RUN` | 6/minute |
| GET /api/debate/status | **30/分钟** 每 IP | `LITCHI_RATE_LIMIT_DEBATE_STATUS` | 30/minute |
| GET /api/debate/result | **30/分钟** 每 IP | `LITCHI_RATE_LIMIT_DEBATE_RESULT` | 30/minute |
| 全局限流开关 | — | `LITCHI_RATE_LIMIT_ENABLED` | 1（开） |

#### 不做的边界
- ❌ 不限其它端点（这期只保 debate/run）
- ❌ 不用 Redis（内存够用）
- ❌ 不区分认证用户/匿名

### triage Git Bash on Windows 兼容模式（~15min ✅）

**问题**：本次会话踩了 5 个 Git Bash 工具链兼容性坑（DNS 缓存、编码、python3 存根、/tmp/ 路径、UTF-8），每次排查浪费 2-5 分钟。

**方案**：

| 文件 | 操作 | 说明 |
|:-----|:----:|:-----|
| `docs/01-guides/triage/git-bash-compat.md` | 🆕 | 5 个模式（DNS/乱码/python3 存根//tmp/路径/GBK编码）+ 通用原则 |
| `docs/01-guides/triage/README.md` | 📝 | +6 行症状速查 + 分类索引加一行 |
| `docs/01-guides/triage/python-runtime.md` | 📝 | 编码问题→交叉引用至 git-bash-compat.md |

### resume-session Skill CI 检查改进（~5min ✅）

**问题**：原命令链 `curl → /tmp/file → python3 → /tmp/file` 在 Windows 上连环踩坑。

**方案**：替换为单条自包含 Python 命令（`py -3 -c "urllib.request → json.loads"`），零外部依赖。

| 文件 | 操作 |
|:-----|:----:|
| `.claude/skills/resume-session/skill.md` | 📝 CI 检查步骤完全重写 + 输出格式加"网络不可达" |

### pre-push hook 流程优化 + 三层测试策略（~15min ✅）

**问题**：pre-push hook 跑 `pytest -v --tb=short`（942 测试 ~10 分钟），每次推送都卡，违反 CHECKS.md 自己写的"全量测试留给 CI"原则。

**方案调研**：先改成了零测试（只跑 lint+type），但业界共识是 pre-push 应跑快测试子集（<2min）。最终采用**三层渐进式验证**：

```
pre-push hook → ruff + pyright + 快测试子集（~35s）    ← 919 个快测试
GitHub CI     → ruff + pyright + 全量测试              ← 23 个慢测试（~600s）
手动 make check → ruff + pyright + 全量测试            ← 大重构前自选
```

| 文件 | 操作 | 说明 |
|:-----|:----:|:-----|
| `pyproject.toml` | 📝 | +`[tool.pytest.ini_options] markers` 定义 slow |
| `tests/test_e2e_full_pipeline.py` | 📝 | `TestE2EFullPipeline` → `@pytest.mark.slow` |
| `tests/test_debate/test_orchestrator.py` | 📝 | `TestDebateOrchestratorRun` → `@pytest.mark.slow` |
| `tests/test_debate/test_d1_cross_review.py` | 📝 | `TestFullFlowWithD1` → `@pytest.mark.slow` |
| `tests/test_debate/test_d3_independent_review.py` | 📝 | `TestFullFlowWithD3` → `@pytest.mark.slow` |
| `tests/test_debate/test_m1_history_injection.py` | 📝 | 3 个类 → `@pytest.mark.slow` |
| `tests/test_integration_master_agent.py` | 📝 | `TestMasterAgentRealLLM` → `@pytest.mark.slow` |
| `scripts/pre-push` | 📝 | 步骤 3/3 `pytest -m "not slow" -x --tb=short` |
| `.git/hooks/pre-push` | 📝 | 重新安装 |
| `CHECKS.md` | 📝 | 三层测试策略文档化 |
| `HANDBOOK.md` 📝 | | hook 范围同步 + 慢测试标记说明 |
| `CI/README.md` 📝 | | 本地拦截原则同步 |
| `WORKFLOW.md` 📝 | | 质量红线同步 |
| `DEVELOPMENT.md` 📝 | | 推送前检查表同步 |

### 文档同步

| 文件 | 操作 |
|:-----|:----:|
| `docs/06-departments/08-backend-api/DEBT.md` | 📝 TD-039 移至已关闭 |
| `docs/06-departments/08-backend-api/HANDOVER.md` | 📝 债务表+优先级更新 |
| `docs/01-guides/HANDOVER.md` | 📝 TD-039 标记已完成，债务数 29→28，pre-push 优化加入效率表 |

## 精度检查

| 检查项 | 结果 |
|:-------|:----:|
| Ruff | ✅ |
| Pyright | ✅ |
| 快测试子集 | ✅ **919 passed, 4 skipped, 23 deselected** — 26s |
| 全量测试 | ✅ **942 passed, 4 skipped** — 0 回归，+1 新测试 |

## 交接建议

下次继续：
1. 🟡 **TD-040 LLM fallback 链** — DeepSeek→OpenAI 自动降级（当前最高优先级未完成项）
2. 🟢 **TD-054 CORS 改环境变量** — ~10min，后端 API 部唯一剩余债务

> **上下文标记**：正常结束
