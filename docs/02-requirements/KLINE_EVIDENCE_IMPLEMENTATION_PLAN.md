# K 线证据完整性实施计划

> 状态：KR-1、KR-2A、KR-2B-1、KR-2B-2A、KR-2B-2B1、2B2A、2B2B、2C 转换核心已完成；KR-2 沪市实网验收待 TD-075
>
> 决策源：[ADR-013 多源证据完整性](../05-decisions/ADR-013-multi-source-evidence.md)
>
> 主债务：`TD-069`
>
> 目标：让盘中 AI 持续工作，同时保证完成日线、动态行情和复权结果不混淆、不静默降级。

## 1. 为什么必须分批实施

K 线门禁会同时影响数据采集、复权、指标、AI 上下文、风控、回测、API 和前端。
一次性替换所有旧链路会放大回归风险。因此本计划采用“先建立事实底座，再逐层切换
消费者”的顺序；每一批都必须保持旧功能可运行，并通过本批验收后才能进入下一批。

本文件负责实施顺序和部门交接；不可违反的业务规则以 ADR-013 为唯一事实源。部门
文档只写各自职责并引用 ADR-013，不自行发明来源、阈值或复权算法。

## 2. 不可违反的统一规则

1. 双源按不同 `upstream_id` 计算。同一机构的不同包装器、SDK、A/B 传输线路只算
   一个事实源。
2. 原始不复权 `RAW` 日线是不可变事实底座；公司行动/复权因子与复权结果分层保存。
3. 完成日线的证券、市场、周期、交易日、币种和复权口径必须完全一致。
4. 完成日线 RAW OHLC 按证券 `price_tick` 规范化后必须完全一致；差一个最小价位
   也进入 `CONFLICTED`。实时行情和分钟战况的经验容差不得套用到完成日线。
5. 成交量统一为“股”。双方均为股精度时要求完全一致；若一方明确只有“手”精度，
   只允许小于一个来源精度单位的差异，并必须暴露精度限制。
6. 成交额按来源声明的单位和精度规范化。在来源精度没有被验证前只做诊断，不用
   任意百分比决定完整性。
7. 前复权成品价格不做跨供应商直接阈值比较。系统先核验 RAW，再核验公司行动、
   基准日和因子，最后由统一算法生成复权序列。
8. 盘中 AI 使用 `FINAL_DAILY + FINAL_MINUTE + LIVE_QUOTE + PROVISIONAL`，不等待
   当日日线收盘；`PROVISIONAL` 永远不能冒充收盘定论。
9. 历史回测只能使用当时已经可获得的点时复权信息，不得用今天的因子改写历史认知。
10. 任一必需层缺失、陈旧、语义不明或冲突时，在首个 LLM 调用前失败关闭，并向用户
    显示具体原因；不得静默选择一个来源继续。

## 3. 来源方案与边界

| 市场 | Phase R 免费阶段 | 完整性结论 | 后续目标 |
|:--|:--|:--|:--|
| 沪深 | 新浪直连 RAW 日线 + 腾讯直连 RAW 日线 | 两个独立上游；进入 TDD 实现 | 交易所授权/持牌主源 + 独立校验源 |
| 北交所 | 新浪已验证；腾讯历史日线未形成可靠第二源 | 第二上游就绪前返回 `INCOMPLETE` | 验证东方财富直连或接入持牌来源 |

`AKShare → 东方财富`、`AData → 东方财富` 和东方财富直连不能算三个来源。来源矩阵
必须记录 `source_id`、`upstream_id`、市场覆盖、字段单位、字段精度、复权语义和
最近烟测时间。

免费公开端点属于 Phase R 的低成本工程方案，不等同于机构级数据保障。系统必须
保留替换为交易所授权或付费来源的接口，不能把供应商名称写进辩论、Agent 或前端
业务逻辑。

## 4. 数据分层和用途

| 层 | 保存内容 | 允许用途 | 禁止用途 |
|:--|:--|:--|:--|
| `RAW` | 原始不复权 OHLCV/成交额、单位、精度、来源时间 | 审计、跨源对账、订单价格核对 | 直接跨除权日计算长期指标 |
| `CORPORATE_ACTION` | 分红、送转、配股、拆并股、因子、基准日、版本 | 复权计算、事件核验 | 无版本覆盖旧因子 |
| `ADJUSTED_QFQ_ASOF` | 指定 `as_of` 基准日的点时前复权 | 盘中技术分析、点时回测 | 当作真实成交价 |
| `ADJUSTED_HFQ` | 后复权研究序列 | 长期财富和收益研究 | 当前下单价格、实时支撑位 |

发生股本变化时，复权成交量按价格因子的反方向调整；纯现金分红只调整价格。
成交额保留 RAW 口径。所有复权输出必须携带 `adjustment_mode`、`factor_source_ids`、
`factor_version`、`reference_date` 和 `as_of`。

## 5. 完成日线对账矩阵

| 检查项 | `complete=True` 条件 | 失败状态/原因 |
|:--|:--|:--|
| 来源独立性 | 至少两个不同 `upstream_id` | `INCOMPLETE / independent_upstream_missing` |
| 证券身份 | 代码、市场、币种一致 | `CONFLICTED / instrument_identity_conflict` |
| 日期集合 | 排除官方停牌后，完成交易日集合一致 | `STALE` 或 `CONFLICTED / trading_date_conflict` |
| RAW OHLC | 按 `price_tick` 规范化后逐字段相等 | `CONFLICTED / raw_ohlc_conflict` |
| RAW 成交量 | 统一为股；差异小于已声明的最低来源精度 | `CONFLICTED / raw_volume_conflict` |
| 成交额 | 单位与精度已知后按精度对账 | 未验证前记录诊断，不参与成功判定 |
| 公司行动 | 事件类型、除权日、基准日和因子版本可追溯 | `CONFLICTED / adjustment_conflict` |
| 今日动态条 | 保持独立 `PROVISIONAL` | 禁止追加到 `final_daily_bars` |

停牌不能靠“缺一行”猜测，必须使用交易日历/证券状态证据。收盘晋升不能只依赖墙钟；
必须同时满足交易阶段已结束、双源返回同一完成交易日、字段对账通过和数据状态稳定。

## 6. 分批实施顺序

### KR-0：规范冻结与引用清理

- ADR-013 固化统一规则；
- 本计划登记部门职责、依赖和验收；
- ROADMAP、全局交接、部门交接与债务统一引用；
- 搜索并区分实时行情 `0.01 元`、分钟累计量 `500 股` 和完成日线严格对账，禁止
  三种阈值互相引用。

**交付条件**：所有相关部门能从各自 HANDOVER 找到同一 ADR、主债务和下一步。

### KR-1：RAW 双源事实底座

#### KR-1A：双源采集与逐日严格对账（✅ 2026-07-30 完成）

- 先写 RED 契约：来源独立性、代码路由、单位/精度、交易日集合、停牌、缺日、
  OHLC 冲突、成交量精度和北交所缺第二源；
- 实现新浪/腾讯沪深 RAW 日线适配器和统一 `KlineEvidenceEnvelope`；
- 保留旧 `DataCollector.get_klines()` 兼容入口，不立即切换全部消费者。

**已交付**：

- `RawDailyBar` 固定 `RAW / CNY / 1d`、市场、最小价位、成交量/额精度；
- 新浪与腾讯声明不同 `upstream_id`；腾讯“手”统一为“股”并保留 100 股精度；
- 完成日线 OHLC 差一个最小价位即冲突，成交量按来源精度对账；
- 当日未完成动态条被排除，带时区窗口为必需输入；
- 沪深真实代表样本通过，北交所因腾讯第二上游缺失明确
  `INCOMPLETE / independent_upstream_missing`；
- 新证据链保持旁路，旧 `DataCollector.get_klines()` 未切换。

#### KR-1B：交易日完备性与审计持久化（🔥 下一原子功能）

- 接入经过批准的交易日历与个股停复牌状态证据，形成请求窗口的“预期完成交易日集”；
- 同时校验“每个来源是否覆盖预期日期”和“两个来源日期是否一致”，禁止两个来源
  同时漏同一天却误判为完整；
- RAW、逐源诊断、原始响应摘要、采集时间和来源版本按 ADR-012 持久化；
- 建立长时间窗分页/覆盖证明，禁止两个来源同时截断却误判为完整。

**当前拆分状态**：

- ✅ **KR-1B-1 官方市场日历与预期日期集**（2026-07-30）：三市场分别保存 2026
  年官方来源、发布时间、规范化闭市日和 SHA-256；共同遗漏官方开市日返回
  `expected_trading_date_missing`，越出已版本化年份返回
  `calendar_coverage_missing`；生产旁路已启用；
- ✅ **KR-1B-2A 官方证券状态契约**（2026-07-30）：版本化覆盖窗口、上市/退市
  边界、全天停牌、盘中临停、来源链接和内容哈希已实现；窗口不完整必须失败；
- ✅ **KR-1B-2B 状态窗口运行时串联**（2026-07-30）：完整官方状态窗口会过滤
  全天停牌/生命周期日期，覆盖不足返回 `security_status_coverage_missing`；
- ✅ **KR-1B-2C-1 上市公司官方停复牌事件采集**（2026-07-30）：复用 CNINFO
  直连分页查询，下载上市公司法定披露 PDF，只有正文明确写出“开市起停牌/复牌”
  及生效日时才生成事件；保存附件 URL 与原始 PDF SHA-256。正文无明确日期、附件
  失败、响应不完整均失败关闭；真实 `300996` 已取得 7 月 27 日停牌、29 日继续
  停牌、30 日复牌证据；
- ✅ **KR-1B-2C-2A 连续状态账本**（2026-07-30）：以官方生命周期、状态检查点
  和连续查询批次为输入，回放窗口前事件，归并重复公告，拒绝批次断档、同日冲突
  与检查点不足；检查点保存已公告但尚未生效的转换，防止跨日丢失次日停牌。输出
  的 `OfficialSecurityStatusWindow` 保留全部来源 URL 与 SHA-256；
- ✅ **KR-1B-2C-2B 官方生命周期与批次/检查点生成**（2026-07-30）：直连上交所
  上市/终止上市清单与深交所 XLSX 清单，保存原始响应 SHA-256；CNINFO 每次完整
  自然日查询输出覆盖区间与响应哈希；账本可生成确定性检查点，并保留尚未生效的
  转换。沪深在市、退市样本及 `300996` 跨批次停复牌链路已真实烟测；
- ✅ **KR-1B-2C-2C 北交所官方状态适配**（2026-07-30）：直连北交所上市公司
  清单、新旧代码对照表和市场日历 JSONP；完整分页、分类计数、查询窗口、产品身份
  与原始响应集哈希均校验。`0600/0700` 进入既有连续账本，`9001` 盘中临停不
  排除全天；当前/920 映射生命周期与新旧代码退市样本已真实烟测。观典防务、泰祥
  股份、翰博高新三家历史转板股缺结构化上市日，明确失败并登记 TD-073，不用首日
  交易提示推断；
- ✅ **KR-1B-3A 审计持久化与点时回放**（2026-07-30）：新增
  `KlineAuditStore`。SQLite 只发布不可变清单和检索索引；逐源 RAW 与规范结果
  写入内容寻址 Parquet。清单保存适配器版本、逐次查询范围/响应哈希/字节数/行数、
  失败诊断、采集时间与日历/生命周期/状态检查点哈希引用。成员先耐久发布、清单后
  事务提交；重复写幂等，成员/清单缺失或篡改均失败关闭。回放同时核对请求身份、
  SQLite 选择列、逻辑快照哈希和 `collected_at <= as_of`，禁止未来证据穿越；
- ✅ **KR-1B-3B 真实长窗覆盖与运行时接入**（2026-07-30）：腾讯按不超过
  1000 个自然日连续分段，保存每段准确响应字节、哈希、行数、时间和覆盖结论；
  新浪单次响应只有在最早原始日期覆盖请求起点时才证明完整，否则以
  `STALE / kline_source_window_not_covered` 失败关闭并保留已解析 RAW。正式
  `collect_and_persist()` 已把逐源证明、规范结果和日历权威引用原子写入 3A
  存储；完整快照强制 canonical 可追溯到成功源 RAW，并按成交量精度、成交额
  可用性/精度和稳定来源 ID 确定性选取，适配器/恢复时钟异常也保留诊断。未经
  用户确认没有换源或加源。

**KR-1 完整交付条件**：KR-1A 已通过；KR-1B 的官方日历/停牌证据、持久化、长窗
覆盖与回放已经落地，下一原子进入 KR-2。当前免费源的能力边界不变：新浪无法证明
任意旧历史窗口、请求年份没有权威日历/状态覆盖时都只会失败关闭，不能进入 AI；
如要让这些窗口成功，必须另行确认数据源与权威版本方案。

##### KR-1B 来源调研结论（2026-07-30）

| 候选 | 实测/文档证据 | 判定 |
|:--|:--|:--|
| 上交所、深交所、北交所年度休市公告 | 三家均发布 2026 年官方休市安排；北交所规则明确周一至周五、法定节假日和公告休市日休市 | **权威事实源**，按交易所和年度版本化保存 |
| 交易所停复牌页面/上市公司公告 | 上交所有结构化停复牌查询；深交所、北交所有交易所/公司停复牌公告 | **权威证据**，只允许有明确代码、起止时间和公告链接的全天停牌排除日期 |
| AKShare `tool_trade_date_hist_sina` | 2026-07-30 实测覆盖 1990-12-19～2026-12-31；真实上游为新浪 | 只作独立校验/发现，不冒充官方日历 |
| AKShare `stock_tfp_em` | 上游为东方财富；传入 `20260417` 实测返回 2026 年 7 月当前停复牌表，日期过滤语义漂移 | **不得参与完整性成功判定**；修复前仅作候选发现 |
| Tushare `trade_cal` / `suspend_d` | 提供统一接口，但交易日历文档只明确 SSE/SZSE；当前环境无包、无令牌，且需积分 | 可选持牌/账户型替代；配置令牌并完成 BSE 覆盖烟测前不得启用 |

**已确认的免费阶段默认方案**：

1. 三家交易所年度休市公告生成按 `market + year + source_url + published_at +
   content_hash` 版本化的本地交易日历；三个市场分别保存，不因日期通常相同而合并
   身份；
2. 新浪交易日历只作差异告警，不能覆盖交易所版本；
3. 某个官方开市日若两家 K 线都缺失，只在交易所停复牌页面或上市公司官方公告
   明确证明该证券全天停牌时排除；无法取得权威证据即失败关闭；
4. 盘中临时停牌不等于整日无日线，不能从预期日线集合剔除；
5. 东方财富停复牌数据只能帮助定位候选公告，不能单独把缺日判成合法停牌；
6. 未来接入授权/付费供应商时实现同一端口，并保留官方公告抽样核验。

实现边界：`src/data/providers/cninfo_status.py` 只输出
`OfficialSuspensionEvent`，不得直接构造 `OfficialSecurityStatusWindow`。这是为了
避免把“本次查询没看到停牌公告”误写成“整段时间确定没有停牌”。事件必须先进入
KR-1B-2C-2A 连续账本和 2C-2B 批次/检查点，再由 KR-1B-3 持久化覆盖证明；
北交所当前显式
`SuspensionEventSourceError`，不得借用沪深路径猜测。

来源依据：
[上交所休市安排](https://www.sse.com.cn/disclosure/dealinstruc/closed/)、
[深交所交易日历](https://www.szse.cn/English/services/trading/calendar/index.html)、
[北交所 2026 年休市公告](https://www.bse.cn/important_news/200027428.html)、
[上交所停复牌信息](https://www.sse.com.cn/disclosure/dealinstruc/suspension/)、
[北交所市场日历](https://www.bse.cn/disclosure/tradingtips.html)、
[Tushare 交易日历](https://tushare.pro/document/2?doc_id=26)、
[Tushare 每日停复牌](https://tushare.pro/document/2?doc_id=214)。

### KR-2：公司行动与统一复权

#### KR-2A：来源无关的因子契约与纯算法（✅ 2026-07-31）

- 新增 `src/data/kline_adjustment.py`，把 `RAW`、公司行动因子和
  `ADJUSTED_QFQ_ASOF` 派生序列保持为不同类型；
- 因子保存 `known_at`、修订号、供应商版本、因子来源/上游和独立公告核验
  来源/上游；来源身份非空、上游必须独立，同版冲突失败关闭；
- 组合 `factor_version` 对实际因子内容和全部血缘做规范化 SHA-256，不能只相信
  供应商自报版本号；
- 现金分红只调整历史价格；送转、拆并股保存精确股本比例与来源声明精度，并以
  有理数校验价量因子；配股和组合事件不误套拆股公式；成交额明确保留 `RAW` 口径；
- `raw_snapshot_id`、`raw_snapshot_as_of`、KR-1 已验证的
  `raw_completed_through` 与查询 `as_of` 同时进入契约；算法不靠 15:00 墙钟猜
  当日日线是否稳定。因子修订只在 `known_at <= as_of` 时生效；
- 算法在内部使用按输入有效位数计算的 Decimal 精度，不受调用方全局 Decimal
  context 影响；模型冻结，避免构造后绕过不变量；
- 26 项合成契约覆盖无事件、现金、送转、3:1 拆股、配股跨停牌、因子修订、
  来源冲突、输入乱序、内容哈希、时区、盘中/收盘边界和 JSON 回放。

KR-2A 只消费已经算好的版本化因子，不负责从公告推导交易所除权公式，也没有接入
任何网络 Provider。交易所公式是 KR-2B 对账依据：
[上交所交易规则](https://www.sse.com.cn/lawandrules/sselawsrules2025/fund/trading/c/c_20260424_10817739.shtml)、
[深交所除权除息说明](https://investor.szse.cn/knowledge/stock/other/t20181017_555756.html)、
[北交所交易规则](https://www.bse.cn/uploads/6/file/public/202109/20210909101516_y5wn2y3ft9.pdf)。

#### KR-2B：因子 Provider 与独立事件核验（🟡 进行中）

**来源决策（方案 A，用户于 2026-07-31 确认）**：

- 沪深因子主源固定为新浪 `qfq.js` 直连累计前复权除数；
- 独立事件核验固定为 CNINFO/交易所正式公司行动披露；
- 北交所即使新浪端点有返回，也在独立官方事件源完成前固定失败关闭；
- Tushare、BaoStock 和 AKShare 包装层不进入当前生产链，新增/换源仍需再次确认。

**KR-2B-1（✅ 2026-07-31）— 累计因子证据快照**：

- `src/data/providers/sina_adjustment.py` 直接解析新浪原始响应，不使用 `eval`；
- 单独声明 `CUMULATIVE_QFQ_FACTOR`，绝不冒充已核验的
  `CORPORATE_ACTION_FACTOR`；它可作为 KR-2B-2B/2C 的因子侧输入，但不能满足后者；
- `QfqFactorSnapshot` 保存原始字节 SHA-256、字节数、适配器版本、采集时间、
  Decimal 原始精度、基准除数和按日期排序的累计除数点；
- 校验响应身份、总数、唯一且严格降序的日期、`1900-01-01` 尾哨兵、最新值为
  1、基准值等于最旧事件值；模型本身再次校验合法 A 股号段、代码/市场、哈希和
  顺序不变量；响应必须保留原始 bytes，日期/因子类型或重复 JSON key 漂移均关闭；
- 网络失败使用 `upstream_request_failed`，脏响应使用
  `invalid_upstream_payload`，北交所使用
  `official_verification_unavailable`，三者不混淆；
- 沪深真实冒烟成功，北交所在触网前返回不支持。

**KR-2B-2A（✅ 2026-07-31）— 官方事件条款契约**：

- `OfficialCorporateActionDocument` 独立保存公告 ID、标题、发布时间、详情 URL、
  PDF URL 和原文 SHA-256；它不是通用 `AnnouncementItem`，也不复用停牌事件；
- `OfficialCorporateActionEvent` 保存公告修订、代码/市场、股权登记日、除权日、
  系统采集时间、CNINFO 血缘、解析器版本和一组规范排序的官方文档；
- 现金金额、行动后/行动前精确股本比例、配股率和配股价使用严格
  Decimal/整数，不接受 Python float 隐式转换；
- 现金、送转、拆股、合股、配股和组合事件分别执行条款矩阵；代码市场不符、
  登记日不早于除权日、公告晚于采集时间、重复公告、非 HTTPS 或单组件组合均
  失败关闭；
- 38 项契约通过。本切片没有网络 Provider、正文正则或因子转换，不能进入 AI。

**KR-2B-2B1（✅ 2026-07-31）— 深市标准权益分派解析与同日聚合**：

- `CninfoCorporateActionSource` 复用完整分页、PDF 下载、PyMuPDF 文本与原 PDF
  SHA-256，只接受深市标准“权益分派实施公告”；
- 锚定最终实施/登记章节；现金选择含税主方案，送股/转增同值去重、异值冲突；
- 同除权日互补公告聚合；分页 total/容量/ID、修订生命周期和条款冲突失败关闭；
- 真实 `000001/1223765073` 得到每股 `0.36200`、2025-06-12 除权日；离线
  `1225352449` 提取布局和 PDF 哈希进入回归夹具；
- SSE、配股、差异化分派未冒充支持，当前显式失败关闭。

**KR-2B-2B2A（✅ 2026-08-03）— 官方模板与双现金口径**：

- 以浦发银行、澜起科技官方 PDF 布局实现 SSE 表格与差异化折算；实际派发
  `distribution_cash_per_share` 和除权 `adjustment_cash_per_share` 永不混用；
- 配股只读取最终发行方案章节，保存精确比例、价格、登记日和除权日；
- 修订账本核心契约保存 `status + supersedes_document_ids`，终止/取消后有效集合为空。

**KR-2B-2B2B（✅ 2026-08-03）— 修订公告联网归链**：

- 把更正、补充、延期、终止、取消、变更和调整公告归入对应实施公告链；
- 更正必须存在唯一被替代公告与更正后完整正文；延期/终止/取消使有效集合为空；
- 当前窗口缺原实施公告时，复用 CNINFO 完整分页接口向前回填 365 天；公告 ID 精确
  边界、发布时间顺序、唯一引用和完整正文任一不满足即失败关闭；
- 2B2B 已完成；2C 转换核心随后于 2026-08-03 完成。整个 KR-2 仍待沪市实网验收。

**KR-2B-2C（✅ 转换核心，2026-08-03）— 累计除数匹配与已核验因子转换**：

- 只有 2B 输出的稳定官方事件才能与新浪相邻累计除数匹配并生成
  `CorporateActionFactor`；
- 累计除数不是 `CorporateActionFactor`。同一除权日匹配后，才把相邻累计值按
  `newer_divisor / older_divisor` 转成 KR-2A 所需的事件价格乘数；事件类型、
  精确股本比例和量因子必须来自官方条款，并按交易所公式核验；
- `known_at = max(新浪快照 collected_at, 官方事件 collected_at)`，禁止用历史
  公告发布时间倒推系统当时已经采集到证据。
- 事件除权日必须唯一命中相邻累计除数；登记日 RAW 必须同证券、日期唯一且收盘价
  为正。事件范围外的更早累计历史不阻塞当前转换；缺匹配、重复或身份错配均失败关闭；
- 价格因子保留新浪 `newer / older` 的完整 Decimal 值。低精度响应按声明量化区间
  传播；16 位响应与官方公式按用户批准的 12 位 `ROUND_HALF_EVEN` 一致性门限核验，
  并把输出 `price_factor_precision` 降为 `1e-12`，不得把未验证尾数冒充精度；
- 现金、送转、配股价款和组合行动统一按交易所公式复算；量因子及精确股本比例只取
  官方条款。版本绑定转换器、完整事件修订和新浪响应哈希；双路来源与较晚采集时间保留；
- 14 项转换契约和 141 项 KR-2 专项通过；真实 `000001` 三次现金分派从新浪、
  CNINFO、腾讯 RAW 全链转换成功，三次价格乘数均以 `1e-12` 验证精度输出。

当前上交所实网公告样本暴露日期表格版式漂移并保持失败关闭，已登记 TD-075。完成
沪市样本兼容、真实沪深修订回放和来源故障烟测后，KR-2 才整体完成。

候选资料：
[AKShare 股票数据文档](https://akshare.akfamily.xyz/data/stock/stock.html)、
[Tushare 复权因子](https://tushare.pro/document/2?doc_id=28)、
[BaoStock 复权因子说明](https://www.baostock.com/helpdocs/pdf/BaoStock%E5%A4%8D%E6%9D%83%E5%9B%A0%E5%AD%90%E7%AE%80%E4%BB%8B.pdf)。

**KR-2 完整交付条件**：同一 RAW + 同一因子版本产生确定性结果；历史点时测试证明
无未来信息；方案 A 的新浪累计除数与 CNINFO/交易所事件通过真实样本对账。仅完成
KR-2B-1 快照不能进入 AI。

### KR-3：双时间尺度业务信封

- 输出 `final_daily_bars`、`final_minute_bars`、`live_quote`、
  `provisional_session_bar`；
- 正式日线指标只消费 `FINAL_DAILY` 的统一前复权序列；
- 可选盘中估算指标使用独立字段和标签；
- 收盘晋升建立幂等状态机，失败可重试，不覆盖已审计 RAW。

**交付条件**：开盘后 AI 输入齐全且无需等待收盘；动态条无法混入完成日线。

### KR-4：辩论、Agent、风控和交易串联

- 辩论引擎在首个 LLM 前检查必需层；
- Agent 提示词和结构化输出保留数据状态、口径和 `as_of`；
- 风控把数据完整性作为独立硬门禁，不用降低置信度代替失败关闭；
- 交易计划的参考价必须来自 `LIVE_QUOTE/RAW`，禁止使用复权价下单；
- 结果记录证据版本，便于复盘。

**交付条件**：所有缺源、错源、冲突、复权冲突测试均证明 LLM/交易调用为零。

### KR-5：API 与前端迁移

- 新证据 API 分栏返回四层数据、复权元数据、逐源诊断和错误码；
- 旧 K 线展示接口保持兼容，前端切换完成后再评估废弃；
- 前端分别展示完成日线、盘中动态、数据时间、复权口径和证据状态；
- 北交所缺第二源、复权冲突和证据陈旧必须是用户可见状态。

**交付条件**：loading/error/empty/offline/data 五态和收盘晋升流程均通过浏览器验证。

### KR-6：回测、存储、监控和全链路验收

- 回测按 `as_of` 读取点时因子，建立未来信息防护；
- Parquet/数据库保存 RAW、公司行动、派生序列和版本血缘；
- 健康检查按市场/来源/能力展示覆盖、延迟、冲突率和最后成功时间；
- 质量保障运行跨部门契约、属性、回放、故障注入和真实烟测；
- 达到机构级来源前，产品界面保留免费来源风险提示。

**交付条件**：全量检查、前端构建、真实沪深样本、北交所失败路径和历史回放全部通过。

## 7. 部门责任和交接点

| 部门 | 主要责任 | 向下游交付 |
|:--|:--|:--|
| 数据管道 | 独立来源、RAW 对账、公司行动、复权、四层信封 | 版本化 Pydantic 契约与诊断 |
| 辩论引擎 | LLM 前完整性门禁、上下文分区 | 零 LLM 失败证据 |
| AI Agent | 状态化措辞、禁止把动态/复权价写成事实成交价 | 带口径与时间的结构化结论 |
| 记忆系统 | 保存结论所用证据版本，不跨版本误召回 | 可重放的证据引用 |
| 风控 | 数据完整性硬门禁和风险告警 | `passed=false` 与明确原因 |
| 交易执行 | 只用实时/RAW 参考价，拒绝复权价和不完整证据 | 可追溯交易计划 |
| 回测研究 | 点时复权、无未来因子、可重复数据快照 | 带数据版本的报告 |
| 后端 API | 分栏响应、稳定错误码、兼容迁移 | 前端可消费契约 |
| 前端 | 状态、时间、口径、冲突和风险可视化 | 用户可见的真实数据身份 |
| 基础设施 | 来源配置、健康指标、告警和密钥/限流 | 可观测运行环境 |
| 质量保障 | 契约矩阵、回放、故障注入、全链路闸门 | 独立验收报告 |

## 8. 平稳迁移和回滚原则

1. 新证据链先旁路运行并记录诊断，验证通过后再成为正式 AI 输入。
2. 旧页面接口在前端迁移完成前保持兼容；不得一次删除所有旧调用。
3. 新链路异常时允许回滚到“禁止启动 AI”，不允许回滚到“残缺数据继续分析”。
4. 每一批单独提交，包含代码、测试、文档、债务、引用清理和学习卡片。
5. 每批完成后执行 `python scripts/check.py --full`；未通过不得进入下一批。

## 9. 最终完成定义

- 沪深完成日线有两个独立 RAW 上游，北交所能力边界真实可见；
- RAW、公司行动和派生复权序列可以独立审计和重放；
- 完成日线、实时、分钟和动态状态无法在类型或字段上互相冒充；
- 盘中 AI 正常工作，证据异常时首个 LLM 前失败关闭；
- 交易不消费复权价，回测不消费未来因子；
- API、前端、日志和健康检查展示同一状态与错误原因；
- 所有部门文档引用 ADR-013 和本计划，不存在第二套阈值或复权定义。
